---
import Layout from "@/components/Layout.astro";
import PageHeader from "@/components/PageHeader.astro";
import Origin from "@/content/codex/01-origin.mdx";
import Roots from "@/content/codex/02-roots.mdx";
import Symbols from "@/content/codex/03-symbols.mdx";
import Spaces from "@/content/codex/04-spaces.mdx";
import Compassion from "@/content/codex/05-compassion.mdx";
import Cycles from "@/content/codex/06-cycles.mdx";

const chapters = [
  { id: "origin", num: "I", title: "The Origin Symphony" },
  { id: "roots", num: "II", title: "Living Lineages" },
  { id: "symbols", num: "III", title: "A Language of Symbols" },
  { id: "spaces", num: "IV", title: "The Practice of Place" },
  { id: "compassion", num: "V", title: "Compassion and Repair" },
  { id: "cycles", num: "VI", title: "Observable Cycles" },
];
---

<Layout title="The Earthsong Codex" description="Six chapters of Earth-based philosophy: origin, ethical learning, symbols, place, compassion, and observable cycles." fullWidth>
  <div class="mx-auto max-w-6xl px-5 py-12 sm:px-6 md:py-16 lg:px-8">
    <PageHeader
      eyebrow="The living text · Six public chapters"
      title="The Earthsong Codex"
      description="A public telling of Earthsong’s foundations: a creation metaphor, an ethic of learning, a language of symbols, accountable relationships with place, compassion, and the observable sky."
    />

    <div class="mt-12 grid gap-12 lg:grid-cols-[250px_minmax(0,1fr)] lg:items-start">
      <aside class="lg:sticky lg:top-28">
        <p class="font-mono text-[10px] font-semibold uppercase tracking-[0.2em] text-ember-dark">Chapters</p>
        <nav aria-label="Codex chapters" class="mt-4 border-t border-ink/15">
          <ol>
            {chapters.map((chapter) => (
              <li class="border-b border-ink/10">
                <a href={`#${chapter.id}`} class="group grid grid-cols-[34px_1fr] gap-3 py-3 text-sm text-stone transition-colors hover:text-ink">
                  <span class="font-mono text-[9px] font-bold tracking-[0.12em] text-ember-dark">{chapter.num}</span>
                  <span>{chapter.title}</span>
                </a>
              </li>
            ))}
          </ol>
        </nav>
        <p class="mt-5 font-body text-xs italic leading-relaxed text-stone-soft">Read in order, or enter wherever the path opens.</p>
      </aside>

      <article class="codex-prose prose prose-lg max-w-none prose-earthsong">
        <section id="origin" class="scroll-mt-28"><Origin /></section>
        <hr class="my-14" />
        <section id="roots" class="scroll-mt-28"><Roots /></section>
        <hr class="my-14" />
        <section id="symbols" class="scroll-mt-28"><Symbols /></section>
        <hr class="my-14" />
        <section id="spaces" class="scroll-mt-28"><Spaces /></section>
        <hr class="my-14" />
        <section id="compassion" class="scroll-mt-28"><Compassion /></section>
        <hr class="my-14" />
        <section id="cycles" class="scroll-mt-28"><Cycles /></section>
      </article>
    </div>

    <div class="mt-16 border-t border-ink/15 pt-6 text-right">
      <a href="#main-content" class="font-mono text-[10px] font-bold uppercase tracking-[0.14em] text-ember-dark">Back to the beginning ↑</a>
    </div>
  </div>
</Layout>

<style>
  .codex-prose :global(section > h2:first-child) {
    margin-top: 0;
    border-top: 0;
    padding-top: 0;
    font-size: clamp(2.75rem, 6vw, 4.75rem);
    line-height: 0.95;
  }

  .codex-prose :global(section > h3) {
    margin-top: 2.6em;
    border-top: 1px solid rgba(15, 15, 13, 0.13);
    padding-top: 1.2em;
    font-size: 2rem;
    line-height: 1;
  }

  .codex-prose :global(section > h4) {
    font-size: 1.45rem;
  }
</style>
